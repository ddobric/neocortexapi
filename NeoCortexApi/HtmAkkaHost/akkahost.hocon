akka {
 debug {  
              receive = on 
              autoreceive = on
              lifecycle = on
              event-stream = on
              unhandled = on
        }
	loglevel=DEBUG
    actor.provider = cluster
	log-config-on-start = on       
    remote {
        dot-netty.tcp {
            port = @PORT
			public-hostname = @PUBHOSTNAME
            hostname = @HOSTNAME
		}
    }
    cluster {
        seed-nodes = [@SEEDHOSTS]
    }	
}

